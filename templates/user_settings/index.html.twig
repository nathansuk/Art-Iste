{% extends 'settingsbase.html.twig' %}

{% block title %}{{ site_name }} : {{ controller_name }}{% endblock %}

{% block settingsspace %}

    {{ form_start(userSettingsForm) }}

    {{ form_widget(userSettingsForm) }}

    <button type="submit" class="btn btn-primary">Modifier</button>
    {{ form_start(userSettingsForm) }}

{% endblock %}
